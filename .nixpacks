[phases.setup]
nixPkgs = ["python3", "ffmpeg", "deno"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install yt-dlp --break-system-packages --quiet"
]

[start]
cmd = "node server.js"
