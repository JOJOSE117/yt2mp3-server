[phases.setup]
nixPkgs = ["python3", "ffmpeg"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install yt-dlp --break-system-packages --target /app/pylib"
]

[start]
cmd = "PYTHONPATH=/app/pylib node server.js"
